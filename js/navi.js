function generateNavigationLinksContacts() {
    generateNavigationLinks(color, linkname);
    generateNavigationLinks(color, linkname);
    generateNavigationLinks(color, linkname);
    generateNavigationLinks(color, linkname);
}